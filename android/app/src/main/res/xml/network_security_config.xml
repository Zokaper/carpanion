<?xml version="1.0" encoding="utf-8"?>
<!--
  Dev-only: allow cleartext (http/ws) traffic to the local test backend so the
  Android emulator can reach a `node server.js` running on the host laptop.
  10.0.2.2 = the emulator's alias for host localhost. Production traffic to
  carpanion.onrender.com stays TLS-only (default), so this is safe to keep.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
